# ModelLang 0.46 conformance

A conforming implementation:

1. implements the complete ModelLang 0.45 language and conformance requirements;
2. changes no source grammar, canonical IR, authority surface, core operation, MCP operation, or target-runtime capability solely for assurance;
3. emits deterministic, schema-valid SML-Agent assessment v1 with exactly ten uniquely identified criteria, conservative statuses, evidence, gaps, overall partial status, and no authority;
4. classifies the assessment as assurance and claims neither complete SML-Agent conformance, agent competence, test execution, nor live-model evidence;
5. provides schema-valid agent evaluation suite and replay v1 formats across the four canonical comparison conditions;
6. validates unique scenarios and complete, unique condition/scenario observations and fails closed for unknown, duplicate, incomplete, or driver-misbound records;
7. scores tool selection, valid plans, effects, calibration, unauthorized attempts, unnecessary reads, policy violations, and optional cost measures deterministically;
8. labels committed scorer fixtures and scorer reports non-empirical and makes no model-quality claim from them;
9. runs deterministic adversarial HTTP, MCP, and live PostgreSQL coverage for identity, authorization, staleness, metadata, delegation, disclosure, request isolation, and operation-kind confusion while retaining all existing runtime enforcement;
10. keeps optional stochastic language-model execution outside conformance and the deterministic health gate;
11. claims neither complete task/effect/reversibility/recovery closure, generated or verified extension implementations, resource subscriptions, prompts, MCP Tasks, complete SML-Agent conformance, nor SML-Federation conformance; and
12. emits compiler/examples 0.46.0 and generator profile `postgresql-http-ui-agent-assurance/30`, adds assessment/evaluation formats v1, and retains canonical IR1, catalog v7, MCP v5, target profile v9, target `/9`, and every existing runtime envelope version.
