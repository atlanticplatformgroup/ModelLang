# ModelLang 0.50 unstable boundaries

The ModelLang 0.49 unstable boundaries remain. In addition, created-row bindings, effect-level return selection, multiple returned rows, repeated updates to one target, partial success, effect-specific retry, cross-database atomicity, and historical IR1 normalization are outside the stable 0.50 contract.
